CI & CD системы

Системы непрерывной интеграции и доставки (CI/CD) автоматизируют сборку, тестирование и развёртывание приложений. Они обеспечивают быструю, безопасную и надёжную доставку кода в production.
🔑 Основные платформы

Jenkins — гибкость и контроль
- Самостоятельно развернутый сервер (on-prem или в облаке)  
- Пайплайны описываются в Jenkinsfile (Declarative или Scripted Pipeline)  
- Богатая экосистема плагинов (>1800)  
- Требует обслуживания: обновления, масштабирование, резервное копирование

Пример (Declarative Pipeline):

GitLab CI — встроенная в репозиторий

- Активируется через файл .gitlab-ci.yml в корне проекта
- Использует runners (можно свои или shared)
- Поддерживает environments, approvals, security scanning

Пример:

GitHub Actions — простота и интеграция

- Работает через workflows в .github/workflows/
- Использует actions (готовые шаги из Marketplace)
- Бесплатно для публичных репозиториев

Пример:

Argo CD — GitOps для Kubernetes

- Реализует принцип GitOps: желаемое состояние хранится в Git
- Автоматически синхронизирует кластер с репозиторием
- Визуальный дашборд + health-check’и

Argo CD не заменяет CI — он отвечает только за CD.  
CI (сборка образа) делается в Jenkins/GitLab/GitHub → Argo CD деплоит манифесты.
🔄 Сравнение подходов

⚠️ Распространённые ошибки

- ❌ «Я использую Argo CD вместо CI»  
    → Argo CD не собирает образы. Нужен отдельный CI для этого.
- ❌ «Храню секреты в .gitlab-ci.yml»  
    → Всегда используй Variables (GitLab) / Secrets (GitHub).
- ❌ «Пайплайн без тестов»  
    → CI без тестов — просто автоматизация, а не гарантия качества.
💡 Главное

Выбор CI/CD зависит от экосистемы:
> - Уже в GitLab? → GitLab CI
- Проект на GitHub? → GitHub Actions
- Много кастомной логики? → Jenkins
- Kubernetes + GitOps? → Argo CD
> Главное — автоматизировать всё, что можно, и никогда не деплоить вручную.

Сноски

[^gitops]: GitOps — подход, при котором Git является единственным источником правды для инфраструктуры и приложений. Argo CD следит, чтобы кластер всегда соответствовал состоянию в репозитории.