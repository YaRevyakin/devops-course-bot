Непрерывная разработка и интеграция (CI/CD)

CI/CD — это сердце DevOps-практики. Это набор автоматизированных процессов, обеспечивающих непрерывную сборку, тестирование и доставку кода в production с минимальным участием человека.
🔑 Ключевые понятия

- CI (Continuous Integration)  
  Каждый коммит в основную ветку запускает автоматическую сборку и тесты. Цель — быстро находить ошибки[^ci].
- CD (Continuous Delivery / Deployment)  
  - Continuous Delivery: код всегда готов к релизу, но деплой — вручную.  
  - Continuous Deployment: каждый успешный пайплайн автоматически попадает в продакшен[^cd].
🔄 Как работает CI/CD-пайплайн?

1. Разработчик делает git push  
2. CI-система (например, GitLab CI) запускает:
   - Сборку образа
   - Unit- и интеграционные тесты
   - Сканирование на уязвимости
3. При успехе — CD-этап:
   - Деплой в staging
   - E2E-тесты
   - Автоматический или ручной деплой в production

Пример: GitHub Actions → Docker build → deploy to Kubernetes.
⚠️ Распространённые ошибки

- ❌ «У нас есть Jenkins — значит, у нас CI/CD»  
  → Без тестов и мониторинга — это просто автоматизация, а не надёжный пайплайн.
- ❌ «Мы не можем делать Continuous Deployment — у нас банковские данные!»  
  → CD возможен при правильных SLO, канареечных релизах и откатах.
💡 Главное

CI/CD — это не про инструменты, а про доверие к автоматизации.  
Чем чаще и надёжнее деплои — тем меньше стресса, выше качество, быстрее feedback.

Сноски
[^ci]: Continuous Integration был популяризирован Extreme Programming (XP). Основная идея: интегрировать код несколько раз в день, чтобы избежать "integration hell".
[^cd]: Continuous Delivery требует полной автоматизации тестирования и инфраструктуры. Без этого — ручной деплой остаётся единственным безопасным вариантом.