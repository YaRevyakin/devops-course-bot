Ключевые моменты

Самое важное из Модуля 3 — кратко, чётко, для повторения и проверки понимания.
🔑 Главные идеи

1. Виртуализация ≠ контейнеризация  
   → Виртуальная машина — полная ОС с гипервизором.  
   → Контейнер — изолированный процесс в общей ОС. Легче, быстрее, но менее изолирован.

2. Docker — стандарт упаковки ПО  
   → Dockerfile → образ → контейнер.  
   → Образ неизменяем, воспроизводим, переносим.

3. Kubernetes управляет жизнью приложений  
   → Pod — минимальная единица.  
   → Deployment — управление репликами и обновлениями.  
   → Service — стабильный доступ внутри кластера.

4. Ingress — шлюз в мир  
   → Позволяет направлять внешний HTTP/HTTPS-трафик в сервисы.  
   → Требует Ingress Controller (NGINX, Traefik).

5. Helm — менеджер приложений для K8s  
   → Упрощает установку, обновление и параметризацию сложных систем (например, Prometheus, GitLab).
⚠️ Проверь себя

- Могу ли я собрать Docker-образ и запустить его?  
- Знаю ли разницу между docker run и Kubernetes Pod?  
- Умею ли я настроить Ingress для веб-приложения?  
- Понимаю ли, зачем нужен отдельный Ingress Controller?

💡 Главный вывод

Современный DevOps строится на контейнерах + оркестрации.  
Docker решает проблему «работает у меня», Kubernetes — проблему «работает везде, всегда и надёжно».