Балансировка нагрузки

Балансировка нагрузки — распределение входящего трафика между несколькими серверами для повышения производительности, масштабируемости и отказоустойчивости. Это ключевой элемент архитектуры любого production-сервиса.

🔑 Основные компоненты

1. Балансировщики: HAProxy и Nginx

HAProxy
- Специализированный L4/L7-балансировщик  
- Высокая производительность, низкая задержка  
- Поддержка health checks, sticky sessions, rate limiting  
- Пример конфига:

Nginx
- Веб-сервер + L7-балансировщик
- Гибкая маршрутизация по URL, заголовкам, cookies
- Часто используется как reverse proxy перед приложениями
- Пример:

Оба поддерживают health checks: автоматически исключают неработающие серверы.

2. Кластеризация и масштабируемость

- Горизонтальное масштабирование: добавление новых узлов вместо усиления одного
- Кластер — группа серверов, работающих как единое целое
- Stateless-архитектура: приложение не хранит данные сессии → легко масштабировать
- Stateful-сервисы (БД): требуют репликации, шардирования, leader election

Пример:  
→ 3 веб-сервера за HAProxy → при росте нагрузки добавляется 4-й → балансировщик сам включает его в пул.
🔄 Как это применяется в DevOps?

- Облако: managed-балансировщики (AWS ALB, Yandex Application Load Balancer)
- Kubernetes: Service типа LoadBalancer или Ingress Controller (Nginx, Traefik)
- Автоматизация: при масштабировании через Terraform/Ansible — обновление пула балансировщика
- Blue/Green и Canary: балансировщик перенаправляет трафик на новую версию
⚠️ Распространённые ошибки

- ❌ «Один балансировщик — достаточно»  
    → Он сам может стать точкой отказа. Используй Keepalived + VIP или managed-решение.
- ❌ «Не настроены health checks»  
    → Балансировщик будет слать трафик на «мертвый» сервер.
- ❌ «Балансирую stateful-приложение без сессий»  
    → Пользователь теряет данные при переходе между серверами. Используй sticky sessions или внешнее хранилище (Redis).
💡 Главное

Балансировка — это не просто «раздать трафик», а обеспечение доступности и качества сервиса.  
Хороший DevOps-инженер проектирует систему так, чтобы она масштабировалась горизонтально и выдерживала сбои отдельных узлов.

Сноски

[^l4-l7]: L4 (transport layer) — балансировка по IP/портам. L7 (application layer) — по HTTP-заголовкам, URL, cookies. Nginx и HAProxy работают на обоих уровнях.