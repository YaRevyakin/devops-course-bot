Проект: Настройка мониторинга

Цель проекта — развернуть систему наблюдаемости на базе Prometheus и Grafana, собрать метрики с сервера и приложения, настроить алерты по ключевым показателям.
🔧 Этапы выполнения

1. Установка стека
- Используй Docker Compose или Helm (в Kubernetes)  
- Минимальный docker-compose.yml:

2. Настройка сбора метрик

- prometheus.yml:

- Убедись, что приложение предоставляет метрики в формате Prometheus (например, через prometheus_client в Python).

3. Настройка Grafana

- Зайди в http://localhost:3000 (логин: admin, пароль: admin)
- Добавь источник данных: Prometheus → URL: http://prometheus:9090
- Импортируй дашборд по ID (например, 1860 — Node Exporter Full)

4. Настройка алертов

- В prometheus.yml добавь правило:
   

- Файл alerts.yml:

- Настрой Alertmanager (опционально) для отправки в Slack/Telegram.
💡 Почему это важно?

Этот проект даёт:
> - Видимость состояния системы в реальном времени
- Раннее обнаружение проблем (до пользователей)
- Основу для SLO и анализа инцидентов
⚠️ Советы

- Начни с готовых экспортеров: node_exporter, cadvisor, blackbox_exporter
- Не создавай алерты на всё — только на то, что влияет на пользователя
- Используй переменные в Grafana для удобной навигации между хостами

Сноски

[^exporter]: Экспортёр — это агент, который преобразует метрики системы или приложения в формат, понятный Prometheus.