Виртуализация

Виртуализация — технология, позволяющая запускать несколько изолированных «виртуальных машин» на одном физическом сервере. Она лежит в основе современных дата-центров и облачных платформ.
🔑 Типы виртуализации

1. Полная (аппаратная) виртуализация
- Гипервизор (например, KVM) эмулирует всё оборудование  
- Гостевая ОС не знает, что работает виртуально  
- Высокая изоляция, но накладные расходы
2. Паравиртуализация
- Гостевая ОС модифицирована для работы с гипервизором  
- Меньше накладных расходов, но требует поддержки ОС  
- Пример: старые версии Xen
3. Контейнеризация (легковесная виртуализация)
- Изоляция на уровне ОС (через cgroups, namespaces)  
- Общее ядро → минимальные накладные расходы  
- Пример: Docker, Podman  
- *Будет подробно в Модуле 4*
🖥️ KVM и QEMU

- KVM (Kernel-based Virtual Machine) — модуль ядра Linux, превращающий его в гипервизор  
- QEMU — эмулятор оборудования, который в связке с KVM обеспечивает полную виртуализацию  
- Вместе они образуют мощную платформу для запуска VM

Команда virt-manager или virsh позволяет управлять виртуальными машинами через CLI/GUI.
☁️ Облачные платформы

Все они предоставляют виртуальные машины как сервис (IaaS), абстрагируя управление железом.
🔄 Как это применяется в DevOps?

- Тестирование: запуск временных VM для CI/CD  
- Изоляция: продакшен, staging, dev — в разных средах  
- Масштабируемость: автоматическое создание/удаление VM под нагрузку  
- Переносимость: образы VM можно переносить между облаками (с ограничениями)
⚠️ Распространённые ошибки

- ❌ «Виртуализация = контейнеры»  
  → Это разные уровни изоляции. VM — полная ОС, контейнер — процесс в ОС.
- ❌ «Я использую только облако — мне не нужно знать KVM»  
  → Облака работают на гипервизорах (часто KVM). Без понимания — слепота при отладке.
💡 Главное

Виртуализация — это абстракция над железом.  
DevOps-инженер должен понимать, как она работает, чтобы эффективно использовать ресурсы и проектировать отказоустойчивые системы.

Сноски

[^kvm]: KVM входит в ядро Linux с версии 2.6.20 (2007). Сегодня — основа AWS, GCP, Yandex Cloud.