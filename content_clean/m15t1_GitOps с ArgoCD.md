GitOps с ArgoCD

GitOps — это подход, при котором состояние инфраструктуры и приложений описывается в Git-репозитории и автоматически применяется к кластеру Kubernetes. ArgoCD — это инструмент, который отслеживает репозиторий, сравнивает его с текущим состоянием кластера и синхронизирует расхождения.

🔑 Ключевые компоненты

- Git-репозиторий  
  Единственный источник правды. Содержит манифесты Kubernetes (Deployment, Service, ConfigMap и т.д.).
- ArgoCD Controller  
  Следит за изменениями в репозитории, сравнивает с реальным состоянием кластера, запускает синхронизацию.
- Application CRD  
  Ресурс Kubernetes, описывающий, какие манифесты применять, из какого репозитория, с какой частотой.
- Sync Policy  
  Автоматический или ручной режим синхронизации. Можно настроить auto-sync + self-heal.
- RBAC / SSO  
  Управление доступом к ArgoCD через LDAP, OAuth, GitHub и др.
⚙️ Как это работает вместе?

Когда ты коммитишь изменение в репозиторий:

1. ArgoCD обнаруживает новый коммит.
2. Сравнивает текущее состояние кластера с желаемым (из Git).
3. Если есть расхождения — запускает синхронизацию (apply манифестов).
4. Обновляет статус Application: OutOfSync → Syncing → Healthy.
5. Отправляет уведомления (Slack, Email) при успехе/ошибке.

→ Без GitOps — деплой хаотичный. С GitOps — всё предсказуемо, аудируемо и воспроизводимо.
⚠️ Распространённые ошибки

- ❌ «Я руками применил манифест — зачем мне ArgoCD?»  
  → Это ломает GitOps-парадигму. Всё должно идти через Git.
- ❌ «Не настроил health-checks — ArgoCD не видит, что приложение упало»  
  → Настрой health.lua для кастомных проверок состояния.
- ❌ «Забыл про RBAC — теперь все могут деплоить»  
  → Используй Project и Role в ArgoCD для ограничения доступа.
💡 Главное

GitOps — это не про инструмент, а про культуру: инфраструктура = код, деплой = pull request, аудит = история коммитов.  
ArgoCD — твой автоматический "деплойщик", который следит за тем, чтобы кластер всегда соответствовал Git.
📌 Сноски

- ArgoCD Documentation — официальная документация.  
- Application — CRD, описывающий приложение в ArgoCD.  
- Self-heal — функция, которая автоматически исправляет расхождения без ручного вмешательства.