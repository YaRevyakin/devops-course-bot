Локальная разработка в Kubernetes

Инструменты для ускорения цикла «написал код → увидел результат» при работе с Kubernetes. 
Позволяют разрабатывать напрямую против локального или удалённого кластера без полного прохождения CI/CD на каждой итерации.

🔑 Ключевые компоненты

- Skaffold  
  Автоматизирует сборку образов, пуш в registry и деплой манифестов при изменении кода. Поддерживает hot-reload через файловые синки.
- Tilt  
  Интерактивная панель с live-логами, зависимостями между сервисами, кастомными шагами (например, запуск тестов) и быстрым обновлением.
- DevSpace  
  Синхронизирует локальные файлы с контейнером в реальном времени (без пересборки образа), поддерживает remote debugging и профили окружений.
- Локальный кластер  
  Minikube, Kind, k3d — легковесные решения для запуска Kubernetes на машине разработчика.
⚙️ Как это работает вместе?

1. Разработчик запускает tilt up или skaffold dev.
2. Инструмент отслеживает изменения в исходниках.
3. При сохранении файла:
   - Либо пересобирается образ и перезапускается под (Skaffold),
   - Либо файл синхронизируется напрямую в работающий контейнер (DevSpace/Tilt).
4. Логи и статус отображаются в реальном времени в терминале или UI.
5. Можно одновременно запускать несколько микросервисов с зависимостями.

→ Без этих инструментов — 5–10 минут на каждый цикл. С ними — 2–5 секунд.
⚠️ Распространённые ошибки

- ❌ «Я использую Skaffold, но не исключил node_modules из sync»  
  → Это вызывает бесконечный цикл перезагрузки. Всегда настраивай .skaffold.yaml с sync.
- ❌ «Запускаю Tilt против production-кластера без namespace isolation»  
  → Опасно! Используй отдельный dev-namespace или локальный кластер.
- ❌ «Не понимаю разницы между rebuild и sync»  
  → Rebuild = новый образ (медленно). Sync = копирование файлов (быстро, но не для всех языков).
💡 Главное

Хорошая локальная среда — это когда ты забываешь, что работаешь с Kubernetes.  
Цель: сделать разработку так же быстрой, как на localhost, но в production-like окружении.

📌 Сноски

- Skaffold profiles — позволяют переключаться между локальным и облачным режимами.  
- Tilt Live Update — аналог hot-reload для Go, Python, Node.js.  
- DevSpace dev mode — запускает контейнер в debug-режиме с открытыми портами.