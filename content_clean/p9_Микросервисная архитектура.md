Проект: Микросервисная архитектура

Цель проекта — развернуть два микросервиса в Kubernetes, настроить между ними связь через Service, добавить API Gateway и базовый мониторинг. Это имитирует production-сценарий с наблюдаемостью и отказоустойчивостью.
🔧 Этапы выполнения

1. Подготовка микросервисов

Сервис A: user-service  
- Отдаёт JSON: {"id": 1, "name": "Alice"}
- Порт: 8000
- Dockerfile:
  

Сервис B: order-service

- При запросе /orders/1 вызывает http://user-service:8000/users/1
- Возвращает: {"order_id": 1, "user": {"id": 1, "name": "Alice"}}
- Порт: 8001

Оба сервиса должны быть stateless и слушать на 0.0.0.0.
apiVersion: v1
kind: Service
meta
  name: user-service
spec:
  selector:
    app: user
  ports:
    - port: 8000
      targetPort: 8000
bash
kubectl apply -f user-service.yaml
kubectl apply -f order-service.yaml
bash
minikube addons enable ingress  # для Minikube
yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
meta
  name: api-gateway
spec:
  rules:
  - http:
      paths:
      - path: /users
        pathType: Prefix
        backend:
          service:
            name: user-service
            port:
              number: 8000
      - path: /orders
        pathType: Prefix
        backend:
          service:
            name: order-service
            port:
              number: 8001
bash
curl http://<INGRESS_IP>/orders/1
Должен вернуть данные с вложенным пользователем
bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm install monitoring prometheus-community/kube-prometheus-stack
```
Проверка:

- Зайди в Grafana (kubectl port-forward svc/monitoring-grafana 3000:80)
- Импортируй дашборд "Kubernetes / Compute Resources / Namespace (Pods)"
- Убедись, что оба сервиса отображаются

Для продвинутого уровня: добавь custom metrics (например, RPS) через prometheus_client.
Сноски

[^service-dns]: В Kubernetes каждый Service автоматически получает DNS-имя вида my-service.namespace.svc.cluster.local. Внутри namespace — достаточно имени сервиса.