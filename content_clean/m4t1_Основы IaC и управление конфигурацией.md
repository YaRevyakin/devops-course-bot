Основы IaC и управление конфигурацией

Инфраструктура как код (Infrastructure as Code, IaC) — это практика управления серверами, сетями и другими ресурсами с помощью описательных файлов, а не ручных настроек. Это обеспечивает воспроизводимость, версионность и автоматизацию.
🔑 Ключевые принципы IaC

1. Декларативный подход
- Ты описываешь желаемое состояние: «хочу 3 сервера с Ubuntu 22.04 и Nginx»  
- Система сама решает, как этого достичь  
- Пример: Terraform, AWS CloudFormation
2. Императивный подход
- Ты описываешь последовательность действий: «установи пакет, запусти службу, скопируй файл»  
- Пример: Bash-скрипты, Ansible (частично)

Современные инструменты стремятся к декларативности — она надёжнее и идемпотентнее.
3. Идемпотентность
- Повторный запуск не меняет результат  
- Если инфраструктура уже в нужном состоянии — ничего не делается
4. Версионность
- Все изменения фиксируются в Git  
- Можно откатиться, сравнить, провести code review
🛠️ Популярные инструменты

🔄 Как это применяется в DevOps?

- Создание сред: dev/staging/prod — из одного шаблона  
- DR (Disaster Recovery): полное восстановление инфраструктуры за часы  
- Безопасность: все изменения проходят через CI/CD и ревью  
- Стоимость: легко удалять временные среды (например, для PR)
⚠️ Распространённые ошибки

- ❌ «Я настроил сервер вручную — потом опишу в коде»  
  → Так возникает «дрейф конфигурации». Всегда сначала код — потом применение.
- ❌ «IaC — только для облака»  
  → Работает и с bare metal, и с виртуалками, и с контейнерами.
💡 Главное

IaC превращает инфраструктуру в продукт, который можно тестировать, ревьюить и доставлять как код.  
Это фундамент всего современного DevOps.

Сноски

[^idempotent]: Идемпотентность — свойство операции, при котором повторный вызов не изменяет результат. Критично для надёжной автоматизации.