Практика

Работа с сетью в Linux — это ежедневная задача DevOps-инженера. От диагностики до настройки безопасности — всё делается через CLI.
🔧 Ключевые команды

Диагностика связности и маршрутизации
- ping — проверка доступности хоста через ICMP  
  
- traceroute — отображение пути к хосту

Работа с DNS
dig — современный инструмент для запросов DNS

nslookup — устаревший, но иногда встречающийся аналог

Проверка портов и соединений
ss — современная замена netstat

netstat — устаревшая, но знакомая многим команда

curl — тестирование HTTP/HTTPS-сервисов

🛠️ Настройка сетевых интерфейсов

Временная настройка (до перезагрузки):

Постоянная настройка — зависит от дистрибутива:
- Ubuntu (Netplan): /etc/netplan/01-netcfg.yaml
- RHEL/CentOS: /etc/sysconfig/network-scripts/ifcfg-eth0
В облаке и контейнерах интерфейсы чаще управляются автоматически.
🔒 Файрволы

UFW (Uncomplicated Firewall) — для Ubuntu

iptables — низкоуровневый фаервол

В новых системах появляется nftables — преемник iptables.
⚠️ Распространённые ошибки

- ❌ «ping работает → сеть в порядке»  
    → ICMP может быть разрешён, а порт 443 — заблокирован. Всегда проверяй конкретный порт (curl, nc).
- ❌ «Я открыл порт в UFW, но сервис недоступен»  
💡 Главное

Сеть диагностируется от низкого уровня к высокому:  
физика → IP → порт → протокол → приложение.  
Умение двигаться по этой цепочке — ключ к быстрому решению проблем.