---
tags:
  - mod9
---
# Что такое микросервисы?

Микросервис — это **небольшое, автономное приложение**, которое реализует одну бизнес-функцию и взаимодействует с другими сервисами через **сетевые интерфейсы**. Вместе они образуют распределённую систему.
## 🔁 Микросервисы vs Монолит

| Критерий | Монолит | Микросервисы |
|---------|--------|--------------|
| **Размер** | Одно большое приложение | Много маленьких сервисов |
| **Развёртывание** | Всё или ничего | Каждый сервис — отдельно |
| **Масштабирование** | Масштабируется целиком | Только нагруженный сервис |
| **Технологии** | Один стек на всё | Любой стек на каждый сервис |
| **Отказоустойчивость** | Падение = полный простой | Падение одного — частичная деградация |
| **Сложность** | Простая архитектура | Сложная сеть взаимодействий |

> Микросервисы **не упрощают код**, но упрощают **масштабирование команд и систем**.

## 📡 Коммуникация между сервисами

### 1. API Gateway
- Единая точка входа для клиентов  
- Отвечает за:  
  - Аутентификацию  
  - Rate limiting  
  - Роутинг запросов к нужным сервисам  
- Примеры: **Kong**, **Traefik**, **AWS API Gateway**

Пример:  
→ Клиент → `api.myapp.com/users` → API Gateway → `user-service`  
→ Клиент → `api.myapp.com/payments` → API Gateway → `payment-service`

### 2. Service Mesh
- Слой **между сервисами** для управления трафиком внутри кластера  
- Решает задачи:  
  - Наблюдаемость (метрики, трассировки)  
  - Безопасность (mTLS)  
  - Отказоустойчивость (retry, circuit breaker)  
- Примеры: **Istio**, **Linkerd**, **Consul Connect**

Как работает:  
→ Каждый Pod получает **sidecar-контейнер** (например, Envoy)  
→ Весь трафик проходит через него → централизованное управление

> **API Gateway** — для внешнего трафика.  
> **Service Mesh** — для внутреннего.

## ⚠️ Распространённые ошибки

- ❌ «Делю монолит на 50 микросервисов сразу»  
  → Начни с 2–3 ключевых доменов. Используй **Strangler Fig Pattern**.

- ❌ «Использую прямые вызовы без таймаутов»  
  → Один зависший сервис может уронить всю цепочку.

- ❌ «Нет API Gateway — каждый сервис публичен»  
  → Это нарушает безопасность и усложняет управление.

## 💡 Главное

> Микросервисы — это **ответ на проблему масштабирования команд**, а не кода.  
> Они требуют **зрелой DevOps-культуры**, иначе превращаются в «распределённый монолит» с удвоенной сложностью.