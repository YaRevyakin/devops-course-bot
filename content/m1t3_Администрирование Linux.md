---
tags:
  - mod1
---
# Администрирование Linux

Администрирование — это ежедневная работа DevOps-инженера: установка ПО, управление службами, контроль ресурсов, безопасность и отладка.
## 🔑 Ключевые задачи

### Управление пакетами
- **Debian/Ubuntu**: `apt update && apt install nginx`  
- **RHEL/CentOS**: `dnf install nginx` или `yum install nginx`  
- **Проверка**: `dpkg -l | grep nginx` / `rpm -qa | grep nginx`
### Службы и systemd
- Запуск: `systemctl start nginx`  
- Автозагрузка: `systemctl enable nginx`  
- Статус: `systemctl status nginx`  
- Логи: `journalctl -u nginx`
### Мониторинг ресурсов
- CPU/RAM: `top`, `htop`  
- Диск: `df -h`, `du -sh /var/log`  
- Сеть: `ss -tuln`, `netstat -tuln` (устаревает)
### Безопасность
- Брандмауэр: `ufw allow 22` (Ubuntu)  
- SELinux/AppArmor: контекстная безопасность (в enterprise-дистрибутивах)  
- Обновления: `apt upgrade` / `dnf upgrade`
### Отладка проблем
- «Сервис не запускается» → `journalctl -u имя_сервиса --no-pager`  
- «Нет места на диске» → `ncdu` или `du -shx /*`  
- «Порт занят» → `lsof -i :80` или `ss -tulnp | grep :80`
## 🔄 Практический пример

Ты деплоишь веб-приложение:
1. Устанавливаешь `nginx` через `apt`  
2. Настраиваешь конфиг в `/etc/nginx/sites-available/`  
3. Включаешь службу: `systemctl enable --now nginx`  
4. Открываешь порт: `ufw allow 80`  
5. Проверяешь логи при ошибке: `journalctl -u nginx -f`
## ⚠️ Распространённые ошибки

- ❌ «Я перезагружу сервер — так быстрее»  
  → В production перезагрузка = downtime. Нужно уметь чинить «на лету».
- ❌ «Я запомню команды»  
  → Используй `alias`, скрипты, документацию. Память — не надёжный инструмент.
## 💡 Главное

> Администрирование — это **не рутина**, а **диагностика и автоматизация**.  
> Хороший DevOps-инженер тратит 10% времени на ручные задачи, 90% — на их устранение.

---
### Сноски

[^systemd]: `systemd` — система инициализации, заменившая `init`. Управляет службами, сокетами, таймерами.