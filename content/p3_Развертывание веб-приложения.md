---
tags:
  - mod3
---
# Проект: Развертывание веб-приложения

Цель проекта — развернуть полноценное веб-приложение в Kubernetes: от сборки Docker-образа до настройки внешнего доступа через Ingress. Это имитирует реальный production-сценарий.
## 🔧 Этапы выполнения

### 1. Подготовка приложения
- Возьми простое веб-приложение (например, на Python/Flask, Node.js или Nginx)  
- Убедись, что оно запускается локально: `python app.py` → `http://localhost:5000`
### 2. Создание Dockerfile
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]
```
- Собери образ:
```bash
docker build -t my-web-app:v1
```
- Протестируй локально:
```bash
docker run -p 5000:5000 my-web-app:v1
```
### 3. Загрузка образа в registry

- Используй **Docker Hub**, **GitHub Container Registry** или **локальный registry**
```bash
docker tag my-web-app:v1 username/my-web-app:v1
docker push username/my-web-app:v1
```
### 4. Развертывание в Kubernetes

- Создай манифесты:

**deployment.yaml**
```yaml
apiVersion: apps/v1
kind: Deployment
meta
  name: web-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    meta
      labels:
        app: web
    spec:
      containers:
      - name: app
        image: username/my-web-app:v1
        ports:
        - containerPort: 5000
```
**service.yaml**
```yaml
apiVersion: v1
kind: Service
meta
  name: web-service
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
```
- Примени:
```bash
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
```
### 5. Настройка Ingress

- Установи Ingress Controller (например, **NGINX Ingress**):
```bash
minikube addons enable ingress  # если используешь Minikube
```
**ingress.yaml**
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
meta
  name: web-ingress
spec:
  rules:
  - host: myapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
```

- Примени и проверь:
```bash
kubectl apply -f ingress.yaml
curl -H "Host: myapp.local" http://<INGRESS_IP>
```

> Для локального теста добавь в *`/etc/hosts`*:  
> `127.0.0.1 myapp.local`
## 💡 Почему это важно?

 Этот проект охватывает **полный цикл доставки**:
 
 - Контейнеризация (Docker)
 - Оркестрация (Kubernetes)
- Сетевой доступ (Ingress)  
>  — всё, что нужно для production-развёртывания.
## ⚠️ Советы

- Если Pod не запускается: `kubectl describe pod <name>`
- Если Ingress не работает: проверь, установлен ли Ingress Controller
- Всегда указывай **конкретные теги образов** (`v1`, а не `latest`)
- Используй `livenessProbe` и `readinessProbe` в продакшене

---
### Сноски

[^ingress]: Ingress — это API-объект Kubernetes, управляющий внешним HTTP/HTTPS-доступом к сервисам. Требует Ingress Controller (NGINX, Traefik и др.).