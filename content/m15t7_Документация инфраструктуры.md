---
tags: [mod15]
---
# Документация инфраструктуры

Живая, актуальная документация как неотъемлемая часть DevOps-процессов. 
Цель — чтобы любой инженер (включая будущего тебя) мог понять архитектуру, процессы восстановления и принципы работы системы за 15 минут.

## 🔑 Ключевые компоненты

- **Markdown + Git**  
  Документация хранится рядом с кодом (в том же репозитории), версионируется, обновляется через PR.
- **Диаграммы (Mermaid)**  
  Архитектурные схемы прямо в Markdown: `graph LR`, `sequenceDiagram`, `flowchart` — без внешних изображений.
- **OpenAPI / Swagger**  
  Спецификация REST API как код. Генерируется автоматически или поддерживается вручную.
- **Runbooks**  
  Пошаговые инструкции на случай инцидентов: «Что делать при падении базы?», «Как откатить релиз?».
- **Wiki-системы**  
  Docusaurus, Wiki.js, Notion — для централизованного поиска, но только если контент синхронизирован с Git.
## ⚙️ Как это работает вместе?

1. При создании нового сервиса — в репозиторий добавляется `docs/`:
   - `architecture.md` с Mermaid-диаграммой,
   - `runbook.md` с действиями при сбоях,
   - `api.yaml` (OpenAPI).
2. В CI запускается проверка: есть ли документация? Если нет — PR не проходит.
3. При мерже — документация автоматически публикуется (например, через GitHub Pages или внутренний портал).
4. Инженер находит всё в одном месте — без расспросов и устных легенд.

→ Без документации — знания уходят с сотрудниками. С ней — система самодостаточна.
## ⚠️ Распространённые ошибки

- ❌ «Я написал документацию в Confluence — она устарела через неделю»  
  → Если не в Git — она мертва. Живая документация = рядом с кодом.
- ❌ «Диаграмма — скриншот из draw.io»  
  → Невозможно обновить без исходника. Используй Mermaid — редактируй как код.
- ❌ «Runbook без тестирования»  
  → Во время инцидента выясняется, что шаг 3 не работает. Тестируй runbooks как код.
## 💡 Главное

> Хорошая документация — это инвестиция в скорость команды.  
> Плохая (или её отсутствие) — скрытый технический долг, который платит каждый новый член команды.
## 📌 Сноски

- Mermaid: https://mermaid.js.org/ — поддерживается в Obsidian, GitHub, GitLab.  
- OpenAPI Spec: https://swagger.io/specification/  
- «Documentation as Code» — ключевой принцип современных DevOps-практик.