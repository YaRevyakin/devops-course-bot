---
tags:
  - mod12
---
# Организация сети, вычислительные мощности, балансировщики

Правильная архитектура в облаке начинается с **сети**, затем — **вычисления** и **балансировка нагрузки**. DevOps-инженер должен уметь проектировать безопасную, масштабируемую и отказоустойчивую инфраструктуру.
## 🌐 Сеть: VPC, Subnets, Security Groups

### VPC (Virtual Private Cloud)
- Изолированная виртуальная сеть в облаке  
- Полный контроль над IP-адресами, маршрутизацией, DNS  
- Пример: `10.0.0.0/16`

### Subnets (подсети)
- Деление VPC на зоны:
  - **Публичные подсети**: для балансировщиков, NAT-шлюзов  
  - **Приватные подсети**: для БД, внутренних сервисов  
- Рекомендуется: **минимум 2 подсети в разных availability zones**

Пример (Yandex Cloud):
```hcl
resource "yandex_vpc_network" "main" {}

resource "yandex_vpc_subnet" "public-a" {
  zone           = "ru-central1-a"
  network_id     = yandex_vpc_network.main.id
  v4_cidr_blocks = ["10.0.1.0/24"]
}

resource "yandex_vpc_subnet" "private-a" {
  zone           = "ru-central1-a"
  network_id     = yandex_vpc_network.main.id
  v4_cidr_blocks = ["10.0.2.0/24"]
}
```
### Security Groups (группы безопасности)

- **Stateful firewall** на уровне ВМ
- Правила: «разрешить входящий трафик на порт 80 с любого IP»
- По умолчанию — **всё запрещено**

> ⚠️ Никогда не открывай порты 22/3389/5432 на весь интернет без ограничений.

---
## 💻 Вычислительные мощности

### Compute Instances (ВМ)

- Гибкость: выбор CPU, RAM, диска, ОС
- Используй **образы с минимальной ОС** (AlmaLinux, Ubuntu Minimal)
- Всегда указывай **SSH-ключ**, отключи парольную аутентификацию

### Managed Services (управляемые сервисы)

|Сервис|Преимущество|
|---|---|
|**Managed Kubernetes**|Нет управления control plane|
|**Managed PostgreSQL/MySQL**|Автоматические бэкапы, репликация, патчи|
|**Object Storage**|Неограниченное, отказоустойчивое хранилище|

> ✅ Используй managed-сервисы, если нет особых требований.  
> ❌ Не запускай БД на той же ВМ, что и веб-сервер.

---
## ⚖️ Балансировщики нагрузки

### Типы балансировщиков

|Тип|Уровень|Использование|
|---|---|---|
|**Network Load Balancer (NLB)**|L4 (TCP/UDP)|Высокая производительность, низкая задержка|
|**Application Load Balancer (ALB)**|L7 (HTTP/HTTPS)|Роутинг по URL, поддержка WebSocket, SSL offload|
### Как работает (пример Yandex Cloud):

1. Пользователь → `https://example.com`
2. Запрос приходит на **Application Load Balancer**
3. ALB проверяет health check → перенаправляет на здоровую ВМ в **публичной подсети**
4. ВМ обрабатывает запрос, при необходимости обращается к БД в **приватной подсети**

> 🔒 БД **никогда не должна быть в публичной подсети**.

---
## 🛡️ Лучшие практики

- **Разделяй зоны ответственности**:
    - Публичная зона: только балансировщик и веб-серверы
    - Приватная зона: БД, кэш, внутренние API
- **Используй managed-сервисы** для БД, Kubernetes, очередей
- **Все правила — через IaC** (Terraform), не через консоль
- **Тестируй failover**: останови ВМ — трафик должен уйти на другую

## 💡 Главное

> Облачная архитектура — это **сетевой дизайн в первую очередь**.  
> Хорошо спроектированная сеть делает систему **безопасной, масштабируемой и отказоустойчивой** по умолчанию.

---
### Сноски

[^stateful-firewall]: Security Groups — stateful: если разрешён входящий трафик, исходящий ответ автоматически разрешён.