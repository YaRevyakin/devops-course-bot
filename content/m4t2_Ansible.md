---
tags:
  - mod4
---
# Ansible

**Ansible** — инструмент управления конфигурацией, позволяющий автоматизировать настройку серверов, развёртывание ПО и оркестрацию. Он работает без агентов, использует SSH и YAML, и поддерживает идемпотентность.
## 🔑 Основные компоненты

### Inventory (инвентарь)
Файл, описывающий целевые хосты:
```ini
[webservers]
web1 ansible_host=192.168.10.10
web2 ansible_host=192.168.10.20

[databases]
db1 ansible_host=192.168.10.30
```
#### Можно использовать динамический inventory (например, из AWS EC2).
### Playbook
**YAML-файл** с последовательностью задач:
```yaml
- name: Установить и запустить Nginx
  hosts: webservers
  become: yes
  tasks:
    - name: Установить nginx
      apt:
        name: nginx
        state: present
    - name: Запустить службу
      service:
        name: nginx
        state: started
        enabled: yes
```
### Roles
Структурированный способ организации кода:
```
roles/
└── nginx/
    ├── tasks/main.yml
    ├── handlers/main.yml
    ├── templates/nginx.conf.j2
    └── vars/main.yml
```
#### Позволяет переиспользовать логику между проектами.

## 🔄 Идемпотентность

**Ansible** гарантирует, что повторный запуск playbook’а **не изменит систему**, если она уже в нужном состоянии.

Пример:
```yaml
- name: Создать файл
  copy:
    content: "Hello"
    dest: /tmp/hello.txt
```
→ Файл будет создан только если его нет или содержимое отличается.

> Это ключевое преимущество перед Bash-скриптами.
## 🔒 Ansible Vault

Шифрование секретов (пароли, ключи, токены):
```bash
# Зашифровать файл
ansible-vault create secrets.yml

# Запустить playbook с vault
ansible-playbook site.yml --ask-vault-pass
```
#### Внутри **`secrets.yml`**:

```yaml
db_password: "supersecret123"
api_token: "xyz789"
```
> Никогда не коммить секреты в Git без шифрования!

## ⚠️ Распространённые ошибки

- ❌ «Я запускаю playbook без `--check`»  
    → Всегда сначала проверяй в режиме dry-run: `ansible-playbook --check`
- ❌ «Использую `shell` вместо модулей»  
    → Модули (`apt`, `copy`, `service`) идемпотентны, `shell` — нет.
- ❌ «Храню пароли в открытом виде»  
    → Используй `ansible-vault` или внешние системы (HashiCorp Vault, AWS Secrets Manager).

## 💡 Главное

> Ansible — это **описание состояния**, а не набор команд.  
> Он превращает управление сотнями серверов в читаемый, безопасный и воспроизводимый процесс.

---
### Сноски

[^idempotent]: Идемпотентность в Ansible достигается за счёт модулей, которые проверяют текущее состояние перед изменением.