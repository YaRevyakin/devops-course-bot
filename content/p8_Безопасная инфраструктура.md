---
tags:
  - mod8
---
# Проект: Безопасная инфраструктура

Цель проекта — создать защищённую среду: настроить сетевой экран, просканировать контейнеры на уязвимости и внедрить централизованное управление секретами через Vault. Это имитирует реальный secure-by-default подход в production.

## 🔧 Этапы выполнения

### 1. Настройка firewall (сетевой безопасности)

#### Вариант A: локально (Ubuntu)
```bash
# Установи UFW
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp      # SSH
sudo ufw allow 80,443/tcp  # HTTP/HTTPS
sudo ufw enable
```
#### Вариант B: в облаке (Yandex Cloud / AWS)

- Настрой **security groups**:
    - Входящий трафик: только 22, 80, 443
    - Исходящий: разрешён
    - Всё остальное — запрещено

> Проверка: `nmap -sV <IP>` — должны быть открыты только нужные порты.
---
### 2. Сканирование контейнеров

- Собери Docker-образ приложения:
```bash
docker build -t myapp:v1 .
```
- Просканируй с помощью **Trivy**:
```bash
trivy image --severity CRITICAL,HIGH myapp:v1
```
- Если найдены уязвимости:
    - Обнови базовый образ (`FROM python:3.11` → `python:3.11-slim`)
    - Обнови зависимости (`pip install --upgrade`)
- Интегрируй в CI (пример для GitHub Actions):
```yaml
- name: Scan for vulnerabilities
  run: |
    trivy image --exit-code 1 --severity CRITICAL myapp:v1
```
→ Pipeline упадёт при критических уязвимостях.

### 3. Внедрение HashiCorp Vault

#### Установка Vault (локально или в Kubernetes)
```bash
# Запуск в dev-режиме (только для обучения!)
vault server -dev -dev-root-token-id="root"
```
#### Сохранение секрета
```bash
export VAULT_ADDR='http://127.0.0.1:8200'
vault kv put secret/myapp/db_password value="supersecret123"
```
#### Использование в приложении

- Через переменные окружения (временно):
```bash
DB_PASSWORD=$(vault kv get -field=value secret/myapp/db_password)
./myapp
```
- В production: используй **Vault Agent** или **Vault CSI Driver** (в Kubernetes) для монтирования секретов как файлов.

> Никогда не храните `root`-токен в коде! Используйте AppRole или Kubernetes Auth.

### 4. Финальная проверка

- Открытые порты — только 22, 80, 443
- Trivy не находит CRITICAL/HIGH уязвимостей
- Приложение получает секреты из Vault, а не из `.env`
- Все изменения зафиксированы в Git (без секретов!)
---
## 💡 Почему это важно?

> Этот проект демонстрирует **три уровня защиты**:
> 
> 1. **Сеть** — firewall/security groups
> 2. **Образы** — сканирование уязвимостей
> 3. **Секреты** — централизованное управление
> 
> Вместе они формируют основу безопасной инфраструктуры.
## ⚠️ Советы

- Для production: используй **TLS** между Vault и клиентами
- Включай **аудит-логи** в Vault: `vault audit enable file file_path=/var/log/vault.log`
- Регулярно ротируй секреты (Vault поддерживает автоматическую ротацию для БД)

---
### Сноски

[^dev-mode]: Dev-режим Vault не шифрует данные на диске и использует in-memory storage. Подходит только для обучения.