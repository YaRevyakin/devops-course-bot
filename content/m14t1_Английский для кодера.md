---
tags:
  - mod14
---
# Техническая лексика: термины, команды, ошибки

Английский в DevOps — это **рабочий инструмент**, а не предмет изучения. Фокус — на понимании и использовании ключевых терминов, команд и сообщений об ошибках.

## 🔑 Обязательная лексика по темам

### 1. Linux и терминал
| Русский          | Английский                                   |
| ---------------- | -------------------------------------------- |
| Запустить        | `start`, `run`, `execute`                    |
| Остановить       | `stop`, `kill`, `terminate`                  |
| Перезапустить    | `restart`                                    |
| Проверить статус | `check status`, `is running?`                |
| Логи             | `logs`, `output`, `stderr/stdout`            |
| Права            | `permissions`, `chmod`, `read/write/execute` |

> Команды всегда на английском:  
> `systemctl restart nginx` → **никогда** не переводится.

---
### 2. Сети
| Термин | Значение |
|-------|--------|
| `latency` | задержка |
| `throughput` | пропускная способность |
| `firewall` | межсетевой экран |
| `port is open/closed` | порт открыт/закрыт |
| `connection refused` | соединение отклонено (сервис не слушает) |
| `timeout` | превышено время ожидания |

---
### 3. Облако и DevOps
| Термин | Значение |
|-------|--------|
| `provision infrastructure` | создать инфраструктуру |
| `deploy` | развернуть |
| `rollback` | откатить |
| `scale up/down` | масштабировать вверх/вниз |
| `high availability (HA)` | высокая доступность |
| `failover` | автоматическое переключение при сбое |

---
### 4. Ошибки и отладка
Частые фразы в логах:
- `Permission denied` → нет прав  
- `Connection timed out` → сетевой таймаут  
- `No such file or directory` → файл не найден  
- `Out of memory (OOM)` → не хватает памяти  
- `ImagePullBackOff` → Kubernetes не может скачать образ

> Умение **читать ошибку** = 80% решения проблемы.

---

## 📚 Где учить?
- **Документация** — читай официальную (Docker, Kubernetes, Terraform)  
- **GitHub Issues** — смотри, как обсуждают проблемы носители  
- **Stack Overflow** — читай вопросы и ответы  
- **RFC и спецификации** — например, HTTP/1.1, Prometheus format

> Не переводи — **привыкай думать на английском**.

---
## 💡 Главное

> Технический английский — это **лексика + контекст**.  
> Ты не обязан говорить идеально, но **должен понимать документацию и ошибки**.  
> Начни с 50 ключевых слов — остальное придёт с практикой.

---
### Мини-чеклист
- [ ] Могу объяснить, что делает `kubectl get pods`  
- [ ] Понимаю, что означает `exit code 1`  
- [ ] Могу прочитать лог Nginx на английском  
- [ ] Знаю разницу между `build` и `deploy`