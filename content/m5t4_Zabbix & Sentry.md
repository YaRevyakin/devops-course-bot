---
tags:
  - mod5
---
# Zabbix / Sentry

Zabbix и Sentry решают разные, но взаимодополняющие задачи:  
- **Zabbix** — мониторинг инфраструктуры (серверы, сети, процессы)  
- **Sentry** — отслеживание ошибок в приложениях (исключения, стектрейсы)
## 🔧 *Zabbix* — мониторинг инфраструктуры

### Что отслеживает:
- CPU, RAM, диск, сеть  
- Состояние служб (`nginx`, `postgresql`)  
- Доступность хостов и портов  
- Пользовательские метрики через агент или скрипты
### Как работает:
- **Сервер Zabbix** собирает данные  
- **Агент** устанавливается на хосты (или используется без агента через SNMP/SSH)  
- **Веб-интерфейс** — дашборды, триггеры, алерты
### Пример использования:
- Триггер: «Если свободная память < 10% → отправить алерт»  
- Автоматическое обнаружение новых хостов в подсети

> Подходит для on-prem, виртуальных машин, физических серверов.
## 🐞 *Sentry* — мониторинг приложений

### Что отслеживает:
- Исключения *(exceptions)* в коде  
- Стек вызовов, переменные, пользовательский контекст  
- Частота ошибок, затронутые пользователи
### Как работает:
- SDK подключается к приложению *(Python, JS, Go и др.)*  
- При ошибках — отправляется репорт в Sentry  
- Веб-интерфейс группирует похожие ошибки, показывает тренды
### Пример:
```python
import sentry_sdk
sentry_sdk.init("https://...@o123456.ingest.sentry.io/123456")
```
→ Любое необработанное исключение автоматически появится в Sentry.

> Идеально для микросервисов, фронтенда, бэкенда.

## 🔄 Сравнение

| Аспект                    | Zabbix                 | Sentry                   |
| ------------------------- | ---------------------- | ------------------------ |
| Уровень                   | Инфраструктура         | Приложение               |
| Данные                    | Метрики, статусы       | Исключения, стектрейсы   |
| Алерты                    | На основе порогов      | На основе частоты ошибок |
| Интеграции                | Slack, Telegram, email | Jira, GitHub, Slack      |
| Самостоятельная установка | Да (open-source)       | Да (open-source + SaaS)  |
## ⚠️ Распространённые ошибки

- ❌ «Использую только *Zabbix* — ошибки в коде не вижу»  
    → *Zabbix* не знает, что приложение вернуло 500, если процесс жив.
- ❌ «Использую только *Sentry* — не вижу, что диск заполнен»  
    → *Sentry* не следит за инфраструктурой.

> Идеальный стек: **Zabbix (инфра) + Prometheus (метрики сервисов) + Sentry (ошибки)**.
## 💡 Главное

> *Zabbix* отвечает на вопрос: **«Жив ли сервер?»**  
> *Sentry* отвечает на вопрос: **«Работает ли логика приложения?»**  
> Оба нужны для полной наблюдаемости.