---
tags:
  - mod1
---
# Операционная система Linux

Linux — это не просто «альтернатива Windows», а **мощная, гибкая и стабильная операционная система**, лежащая в основе большинства серверов, облаков и контейнеров. Понимание её внутреннего устройства критично для DevOps-инженера.
## 🔑 Ключевые компоненты

- **Ядро (Kernel)**  
  Управляет CPU, памятью, устройствами, файловыми системами.  
  Пример: `Linux 5.15.0-91-generic`.
- **Процессы**  
  Каждая программа — процесс с PID, владельцем, приоритетом.  
  Управляются через `ps`, `top`, `kill`, `systemd`.
- **Файловые системы**  
  - `ext4` — стандарт для дисков  
  - `tmpfs` — в RAM  
  - `overlayfs` — используется в Docker[^overlayfs]
- **Загрузка ОС**  
  BIOS/UEFI → bootloader (GRUB) → kernel → init (systemd) → сервисы.
- **Пользователи и права**  
  - `root` — суперпользователь  
  - Права: `rwx` (read, write, execute)  
  - Группы: `usermod -aG docker $USER`
## 🔄 Как это работает вместе?

Когда ты запускаешь `docker run nginx`:
1. Ядро создаёт процесс  
2. Файловая система монтирует образ через `overlayfs`  
3. Сеть настраивается через `iptables`/`nftables`  
4. Пользовательские права ограничивают доступ

→ Без понимания этих слоёв — ты «слеп» при отладке.
## ⚠️ Распространённые ошибки

- ❌ «Я не трогаю ядро — мне это не нужно»  
  → Ошибки в памяти, сети, дисках — всегда связаны с ядром.
- ❌ «Права 777 решат проблему»  
  → Это дыра в безопасности. Нужно понимать принцип наименьших привилегий.
## 💡 Главное

> Linux — это **живая система**, где всё взаимосвязано.  
> DevOps-инженер должен уметь «читать» её состояние через CLI.
---
### Сноски
[^overlayfs]: OverlayFS — объединяющая файловая система, позволяющая накладывать слои (как в Docker). Основа образов контейнеров.