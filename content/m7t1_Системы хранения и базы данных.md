---
tags:
  - mod7
---
# Системы хранения и базы данных

В облаке данные — это не просто файлы на диске, а **управляемые сервисы**, которые обеспечивают надёжность, масштабируемость и безопасность. DevOps-инженер должен понимать типы хранилищ и уметь выбирать правильное решение под задачу.
## 🔑 Типы систем хранения

### 1. Блочное хранилище **(Block Storage)**
- Работает как «виртуальный диск», подключаемый к ВМ  
- Низкая задержка, высокая производительность  
- Используется для:  
  - Баз данных  
  - Системных дисков  
- Примеры:  
  - AWS EBS  
  - Yandex Cloud Compute Disk  
  - GCP Persistent Disk

> ❗ Не разделяется между несколькими ВМ одновременно (за исключением специальных режимов).
### 2. Файловое хранилище **(File Storage)**
- Доступ по протоколам NFS/SMB  
- Может монтироваться на **несколько ВМ** одновременно  
- Используется для:  
  - Общих каталогов  
  - Медиафайлов  
- Примеры:  
  - AWS EFS  
  - Azure Files  
  - Yandex File Storage
### 3. Объектное хранилище **(Object Storage)**
- Хранит данные как «объекты» с уникальным ключом (URL)  
- Высокая отказоустойчивость, практически неограниченный объём  
- Используется для:  
  - Бэкапов  
  - Статики (картинки, JS, CSS)  
  - Логов  
- Примеры:  
  - AWS S3  
  - Google Cloud Storage  
  - Yandex Object Storage (S3-совместим)

> ✅ Идеально для immutable-данных.  
> ❌ Не подходит для БД (нет случайного доступа).

## 🗄️ Управляемые базы данных (Managed DB)

Облака предоставляют **сервисы баз данных без управления инфраструктурой**:

| Тип | Примеры | Когда использовать |
|-----|--------|-------------------|
| **Реляционные** | PostgreSQL, MySQL, SQL Server | Когда нужны транзакции, ACID, связи |
| **NoSQL** | MongoDB, DynamoDB, Firestore | Гибкая схема, горизонтальное масштабирование |
| **In-memory** | Redis, Memcached | Кэширование, сессии, очереди |
| **Аналитические** | ClickHouse, BigQuery, Redshift | OLAP, отчёты, аналитика |

Преимущества managed-БД:
- Автоматические бэкапы  
- Репликация и failover  
- Патчи и обновления  
- Мониторинг «из коробки»

## ⚠️ Распространённые ошибки

- ❌ «Храню бэкапы в блочном хранилище»  
  → Используй **объектное хранилище** — дешевле и надёжнее.
- ❌ «Запускаю PostgreSQL на обычном диске»  
  → Для БД нужен **высокопроизводительный SSD-диск** (иначе latency убьёт производительность).
- ❌ «Использую объектное хранилище как диск»  
  → Нельзя монтировать S3 как `/mnt` напрямую. Используй только через API или специальные драйверы (s3fs — с осторожностью).
## 💡 Главное

> Выбор системы хранения — **архитектурное решение**.  
> Правильный выбор снижает стоимость, повышает надёжность и упрощает эксплуатацию.

---
### Сноски

[^s3-compatible]: Большинство инструментов (rclone, restic, backup scripts) работают с любым S3-совместимым хранилищем — включая Yandex Object Storage.