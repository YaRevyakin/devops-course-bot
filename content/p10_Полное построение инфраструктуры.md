---
tags:
  - mod10
---
# Проект: Полное построение инфраструктуры

Цель проекта — развернуть выделенный сервер, установить ISPmanager, настроить веб-сайт и автоматизировать резервное копирование. Это имитирует типичный сценарий для хостинга небольшого бизнеса или фрилансера.
## 🔧 Этапы выполнения

### 1. Подготовка сервера

- Закажи VPS (например, в Hetzner, Selectel, или Yandex Cloud)  
- ОС: **AlmaLinux 9** или **Debian 12** (чистая установка, без предустановленного ПО)  
- Открой порты: **22 (SSH)**, **80/443 (HTTP/HTTPS)**, **1500 (ISPmanager)**

Подключись:
```bash
ssh root@<server_ip>
```
⚠️ Убедись, что нет Apache/Nginx/MySQL — ISPmanager требует «чистую» систему.

---
### 2. Установка ISPmanager
```bash
wget https://download.ispsystem.com/install.sh
bash install.sh
```
- Выбери версию: **ISPmanager 6**
- Режим: **Lite** (если один сайт) или **Host** (мультисайт)
- Следуй инструкциям в терминале

После установки открой в браузере:  
→ `https://<server_ip>:1500`  
→ Создай администратора

---
### 3. Настройка сайта

В веб-интерфейсе ISPmanager:

1. **Добавь домен**:
    - Имя: `example.com`
    - Директория: `/var/www/example.com/`
2. **Включи SSL**:
    - Let’s Encrypt → автоматическая выдача сертификата
3. **Загрузи сайт**:
    - Через File Manager или SFTP (используй учётную запись пользователя, созданного для домена)
    - Пример `index.html`:
- ```html
  <h1>Мой сайт работает!</h1>
  ```
> Если используешь локальный домен — добавь запись в DNS или в `/etc/hosts`.
---
### 4. Автоматизация бэкапов

В ISPmanager:

1. Перейди в **«Резервные копии» → «Планировщик»**
2. Настрой задачу:
    - **Что архивировать**: файлы сайта + базы данных
    - **Периодичность**: ежедневно в 02:00
    - **Хранение**:
        - Локально: `/backup/` (ограничено местом на диске)
        - **Удалённо**: FTP / SFTP / WebDAV (рекомендуется)
3. **Включи шифрование** (опционально, но желательно):
    - Укажи пароль для архива
4. **Протестируй восстановление**:
    - Вручную запусти бэкап
    - Удали файл сайта
    - Восстанови из архива через интерфейс

---
### 5. Базовая безопасность

- Включи **двухфакторную аутентификацию** в профиле администратора
- Ограничь IP-адреса для входа в панель (если возможно)
- Обнови систему:
```bash
dnf update -y  # AlmaLinux
# или
apt update && apt upgrade -y  # Debian
```
## 💡 Почему это важно?

> Этот проект охватывает **полный цикл управления традиционным хостингом**:
> 
> - От «голого» сервера до работающего сайта
> - С безопасностью (SSL) и надёжностью (бэкапы)
> 
> Хотя это не DevOps в enterprise-стиле, такие навыки востребованы в SMB-сегменте.

## ⚠️ Советы

- Не используй root-доступ через панель — создай отдельного пользователя
- Регулярно проверяй, **работают ли бэкапы**
- Для production-сайтов лучше использовать **CI/CD + IaC**, а не ISPmanager
---
### Сноски

[^sftp-backup]: SFTP-бэкапы безопаснее FTP, так как трафик шифруется. Используй их, если удалённый сервер поддерживает SSH.