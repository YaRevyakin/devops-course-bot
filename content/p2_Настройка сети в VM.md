---
tags:
  - mod2
---
# Проект: Настройка сети в VM

Цель проекта — получить практический опыт настройки сетевого взаимодействия между виртуальными машинами. Это основа для понимания VPC, микросервисов и сетевой безопасности в облаке.
## 🔧 Задачи

1. **Развернуть две виртуальные машины**  
   - Используй VirtualBox, VMware, QEMU/KVM или облачные инстансы (Yandex Cloud, AWS EC2)  
   - ОС: Ubuntu 22.04 LTS

2. **Настроить сеть в режиме «внутренней сети» или «приватной подсети»**  
   - В VirtualBox: тип адаптера — **Internal Network**  
   - В облаке: размести обе VM в одной приватной подсети (без публичных IP)

3. **Назначить статические IP-адреса**  
   - VM1: `192.168.10.10/24`  
   - VM2: `192.168.10.20/24`

4. **Проверить связность**  
   - С VM1: `ping 192.168.10.20`  
   - С VM2: `ping 192.168.10.10`

1. **Запустить веб-сервер и протестировать через curl**  
   - На VM1: 
```bash
python3 -m http.server 8000
```
   - На VM2:  
   ```bash
curl http://192.168.10.10:8000
   ```
   - Должен вернуться HTML-код директории.
## 🎯 Минимальный рабочий результат

- Обе VM видят друг друга по ping  
- HTTP-запрос проходит успешно  
- Нет публичного доступа из интернета (только внутренняя сеть)
## 💡 Почему это важно?

> Этот проект моделирует **реальную архитектуру**:  
> - База данных и приложение в одной приватной подсети  
> - Публичный трафик идёт только через балансировщик  
> - Безопасность достигается за счёт сетевой изоляции

Ты учишься:
- Работать с сетевыми интерфейсами  
- Настраивать маршрутизацию (если нужно)  
- Диагностировать проблемы на уровне IP и портов
## ⚠️ Советы

- Если *ping* не работает:  
  → Проверь, что обе VM в одной internal network  
  → Убедись, что фаервол отключён (`sudo ufw disable`)  
  → Выполни `ip a` на обеих машинах — правильные ли IP?

- Если *curl* не работает:  
  → Убедись, что сервер слушает на `0.0.0.0`, а не `127.0.0.1`  
  → Проверь: `ss -tuln | grep :8000`

---
### Сноски

[^internal-network]: Internal Network в VirtualBox изолирует VM от хоста и внешнего мира — идеально для безопасного тестирования.