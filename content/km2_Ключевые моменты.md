---
tags:
  - mod2
---
# Ключевые моменты

Самое важное из Модуля 2 — кратко, чётко, для повторения и проверки понимания.
## 🔑 Главные идеи

1. **Сеть — иерархия уровней**  
   → От физического соединения до HTTP-запроса. DevOps должен уметь «спускаться» по уровням при отладке.

2. **IP-адресация и подсети — основа изоляции**  
   → Приватные диапазоны (`192.168.0.0/16`, `10.0.0.0/8`) используются везде: от локальных сетей до облаков.

3. **TCP vs UDP — выбор протокола влияет на надёжность**  
   → Базы данных, SSH, HTTP — TCP. DNS, стриминг — UDP.

4. **Диагностика начинается с простого**  
   → `ping` → `traceroute` → `ss`/`netstat` → `curl`/`dig`. Пошаговая проверка — ключ к быстрому решению.

5. **Безопасность = контроль трафика**  
   → Фаерволы (`ufw`, `iptables`) ограничивают доступ. В облаке — security groups и network ACLs.

## ⚠️ Проверь себя

- Могу ли я объяснить разницу между `/24` и `/16`?  
- Знаю ли, как проверить, слушает ли сервис на всех интерфейсах (`0.0.0.0`) или только на localhost?  
- Умею ли я настроить две VM так, чтобы они общались, но были недоступны из интернета?

## 💡 Главный вывод

> Сеть — это не «магия», а **логическая структура с правилами**.  
> Понимание её принципов позволяет строить безопасные, масштабируемые и наблюдаемые системы.